# Expected Output

The answer should prefer `.output(...)` plus tuple `ensure` for fixed required leaves, mention that `.output(...)` defaults to `format="auto"` with conservative format selection, recommend explicit `flat_markdown` for flat string outputs containing large code/HTML/Markdown blocks, recommend explicit `hybrid` for prose/code fields mixed with structured lists or objects when retry latency is acceptable, recommend explicit `format="json"` for judges, booleans, numbers, dense nested data, or downstream JSON-only contracts, and recommend plain text without `.output(...)` for a single freeform artifact such as an article, email, report, Markdown page, or HTML page. It should explain that `instant` streaming is available for `json`, `flat_markdown`, `hybrid`, and resolved `auto` structured outputs, but plain text should use `delta` streaming; it should treat instant events as provisional UI state and final `get_data()` as the durable result. When model reliability matters, it should explain that `max_retries=3` can recover ordinary parse/key omissions with up to three additional model attempts, but complex nested arrays, placeholder echo, prose in boolean/numeric fields, truncation, and many wildcard ensure paths can still fail after retries. It should use runtime `ensure_keys` only for conditional path-presence checks, and use `.validate(...)` / `validate_handler=` for business-rule checks instead of custom parsers or handwritten retry loops. When the task is testing model-owned semantic content, recommend a second Agently model-judge request with output control and boolean rule judgments instead of keyword, substring, regex, or snapshot assertions. If the user needs later retries to change prompt options or settings, the answer may mention the advanced path of writing through `context.prompt.set(...)` or `context.settings.set(...)`, while noting that mutating values returned by `get()` is not the stable write path.
