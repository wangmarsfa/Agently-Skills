# Expected Output

The answer should prefer `.output(...)` plus tuple `ensure` for fixed required leaves, mention that omitted `.output(..., format=...)` reads `prompt.default_output_format` and that the framework default is `json`; it should explain that explicit `format="auto"` or `prompt.default_output_format="auto"` uses structural selection, where flat string-only dict schemas resolve to `xml_field`, dict schemas that mix string fields with typed non-string fields resolve to `hybrid`, and all-control/all-complex/non-dict schemas resolve to `json`. It should mention that `yaml_literal` is explicit opt-in and `flat_markdown` is explicit-only compatibility mode. It should recommend explicit `format="json"` for compact all-typed data or downstream JSON-only contracts; explicit `format="xml_field"` for flat string-only dict schemas when XML-like field envelopes are useful; explicit `format="hybrid"` for mixed long text plus typed fields after provider/model stability checks; explicit `format="yaml_literal"` only when YAML is intentionally desired; and plain text without `.output(...)` for a single freeform artifact such as an article, email, report, Markdown page, or HTML page. It should mention that recent qwen2.5:7b checks found hybrid can omit section headers or echo old scaffold comments, so untested local models should keep the default at `json`. It should explain that tuple `True` and runtime `ensure_keys` check path/key presence only, so `None`, blank strings, `False`, `0`, empty lists, and other intentionally empty values remain valid when the path exists; it should recommend tuple `"not_null"` only when a required path must also contain a meaningful value, where `None`, blank strings, empty lists or wildcard matches, and lists containing missing required values fail and share the retry budget while `False` and `0` remain valid typed values. It should explain that `instant` streaming is available for `json`, `flat_markdown`, `hybrid`, `xml_field`, `yaml_literal`, and resolved `auto` structured outputs, but plain text should use `delta` streaming; it should treat instant events as provisional UI state and final `get_data()` as the durable result. When model reliability matters, it should explain that `max_retries=3` can recover ordinary parse/key omissions with up to three additional model attempts, but complex nested arrays, placeholder echo, prose in boolean/numeric fields, truncation, and many wildcard ensure paths can still fail after retries. It should use runtime `ensure_keys` only for conditional path-presence checks, and use `.validate(...)` / `validate_handler=` for business-rule checks instead of custom parsers or handwritten retry loops. It should explain that provider-native reasoning and leading outer `<think>...</think>` belong in `reasoning_delta`/`reasoning_done` events while `original_delta`/`original_done` preserve raw content and payload-internal `<think>` is ordinary answer content. When the task is testing model-owned semantic content, recommend a second Agently model-judge request with output control and boolean rule judgments instead of keyword, substring, regex, tokenization, or snapshot assertions. If the user needs later retries to change prompt options or settings, the answer may mention the advanced path of writing through `context.prompt.set(...)` or `context.settings.set(...)`, while noting that mutating values returned by `get()` is not the stable write path.
