# Expected Output

The answer should prefer `.output(...)` plus tuple `ensure` for fixed required leaves, mention that `.output(...)` defaults to `format="auto"` with flat/hybrid markdown heuristics, recommend `flat_markdown` for flat structured outputs containing large code/HTML/Markdown blocks, recommend `hybrid` for prose/code fields mixed with structured lists or objects, recommend explicit `format="json"` when downstream code needs the legacy JSON-only contract, and recommend plain text without `.output(...)` for a single freeform artifact such as an article, email, report, Markdown page, or HTML page. It should explain that `instant` streaming is available for `json`, `flat_markdown`, `hybrid`, and resolved `auto` structured outputs, but plain text should use `delta` streaming; it should treat instant events as provisional UI state and final `get_data()` as the durable result. When model reliability matters, it should mention the observed 2026-05-23 compatibility results: 72/72 auto checks passed, hybrid had 24/24 native parse success with 0 degradations, and flat_markdown had 9 auto degradations rescued by JSON retry, concentrated in numeric/header-adherence cases. It should use runtime `ensure_keys` only for conditional path-presence checks, and use `.validate(...)` / `validate_handler=` for business-rule checks instead of custom parsers or handwritten retry loops. When the task is testing model-owned semantic content, recommend a second Agently model-judge request with output control and boolean rule judgments instead of keyword, substring, regex, or snapshot assertions. If the user needs later retries to change prompt options or settings, the answer may mention the advanced path of writing through `context.prompt.set(...)` or `context.settings.set(...)`, while noting that mutating values returned by `get()` is not the stable write path.
