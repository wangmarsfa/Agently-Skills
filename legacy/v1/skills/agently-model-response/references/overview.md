# Overview

This skill owns `get_response()`, parsed data access, metadata access, and response streaming consumption. Prefer async getters and async generators by default; treat sync readers as wrappers for simple local usage.
